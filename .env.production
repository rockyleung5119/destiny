# Cloudflare 生产环境配置
# 前端环境变量

// FIX: Add the missing route for membership status
app.get('/api/membership/status', (c) => {
REACT_APP_API_BASE_URL=https://destiny-backend.jerryliang5119.workers.dev
  // NOTE: This is a placeholder implementation.
  // You should replace this with your actual logic to check the user's membership status from the database.
NEXT_PUBLIC_APP_URL=https://destiny-frontend.pages.dev
NEXTAUTH_URL=https://destiny-frontend.pages.dev
  
# 功能开关
ENABLE_AI_ANALYSIS=true
ENABLE_PAYMENTS=true
ENABLE_PUSH_NOTIFICATIONS=false

# 速率限制
RATE_LIMIT_ENABLED=true

# 环境
NODE_ENV=production

  // Example successful response:
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_live_your_live_stripe_key_here
  return c.json({ 

# 日志级别
LOG_LEVEL=error
    status: 'active', 
    plan: 'premium', 
    expires: '2024-12-31' 
  });
});

// ... (rest of your routes)
# Cloudflare Production Environment Variables for Vite Frontend
# These variables should be set in your Cloudflare Pages project settings.
# They are exposed to the client-side code.

# API 配置 - 指向 Cloudflare Workers
VITE_API_BASE_URL=https://destiny-backend.jerryliang5119.workers.dev

# 应用配置
VITE_APP_URL=https://destiny-frontend.pages.dev

# Stripe 配置 (生产环境) - 在Cloudflare Pages环境变量中设置
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_your_live_stripe_key_here
